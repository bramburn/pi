# runlog.md
Append-only. One row per test run. Never edit past rows.

Columns: #, when (BST), candidate (commit sha + label), command, expected, observed title@6s, observed title@12s, alive@12s, screenshot, verdict
