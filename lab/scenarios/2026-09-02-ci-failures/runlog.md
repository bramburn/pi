# Runlog — 2026-09-02-ci-failures

Append-only. One row per experimental run or per inspection. Never edit past
rows; add a new row to supersede.

| Run | Date (BST) | Commit | Status | Notes |
| --- | --- | --- | --- | --- |
| 0 | 2026-09-02 20:48 | (scenario only) | scaffolded | Created scenario folder, README, experiment.toml. No code yet. |
| 1 | 2026-09-02 20:50 | (inspection) | measured | Pulled the GitHub Actions run list for bramburn/pi. Found 2 `ci.yml` failures in the last hour: run 33675057545 (main, push 19:43:53Z) and run 33674889692 (issue-with-subagent, push 19:42:10Z). Pulled the PR list — 4 open PRs in scope (#880, #877, #876, #873). |
| 2 | 2026-09-02 20:55 | fb62a169a (local) | measured | `cat .github/workflows/ci.yml` shows lines 45-50 of the current main branch are at column 1 instead of column 9. The `run: bun install --ignore-scripts --linker=hoisted` line and its 5-line `# --linker=hoisted` comment block are unindented, so the YAML is structurally broken. |
| 3 | 2026-09-02 20:57 | (parse check) | measured | `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` raises `yaml.parser.ParserError: while parsing a block mapping ... in line 1, column 1, expected <block end>, but found '<block mapping start>' in line 50, column 2`. Independent `node -e` check using the `yaml` package reports: `All mapping items must start at the same column at line 50, column 2`. Two parsers agree on the same broken line. |
| 4 | 2026-09-02 21:00 | 33675057545 (web fetch) | measured | The GitHub Actions UI annotation on the failed run reads verbatim: **"Invalid workflow file: .github/workflows/ci.yml#L49 — You have an error in your yaml syntax on line 49"**. The `#L49` annotation points at the last comment line; the actual parse error is on line 50 (the `run:`). Both refer to the same indentation block. |
| 5 | 2026-09-02 21:03 | 026d74e86 (PR #880 head) | measured | PR #880's branch has the YAML correctly indented. The 5-line `--linker=hoisted` comment block plus the `run: bun install --ignore-scripts --linker=hoisted` line are at column 9 (matching the `step: Install dependencies` parent). Confirmed via `git show fork/fix/followups-after-pr-879:.github/workflows/ci.yml`. **The fix already exists in PR #880.** |
| 6 | 2026-09-02 21:05 | 33664432592 (PR #880 CI) | measured | PR #880's `build-check-test` ran to step 8 (`Test`) and failed. Steps 1-7 (Set up job, Checkout, Setup Bun, Install system dependencies, Install dependencies, Build, Check) all succeeded. The YAML fix is working. The Test step took 3m21s. |
| 7 | 2026-09-02 21:07 | 33664432592 (test annotations) | measured | 4 test failures annotated on the failed Test step: (a) `session-backends/sqlite-node/test/repository.test.ts#L296` — pre-existing; (b) `session-backends/sqlite-node/test/migrations.test.ts#L15` — NEW; (c) `session-backends/sqlite-node/test/facts-query.test.ts#L23` — pre-existing; (d) `coding-agent/test/interactive-mode-compaction.test.ts#L224` — pre-existing. See D9. |
| 8 | 2026-09-02 21:10 | 9bc6bce38..fb62a169a (diff) | measured | `git diff` confirms `packages/session-backends/sqlite-node/src/sqlite/migrations/002_add_session_name.sql` is a new file added by the merge. The PR-871 merge that broke ci.yml's YAML also added the 002 migration, which is what causes the `migrations.test.ts#L15` failure — the test fixture was not updated. |
| 9 | 2026-09-02 21:15 | (decision pending) | blocked | Awaiting user direction. Three options on the table: (a) merge PR #880 as-is and address the 4 test failures in follow-up PRs; (b) rebase a fresh YAML-only fix branch and merge that first to unbreak main, leaving the 4 test failures on PR #880; (c) fix the migrations.test fixture (line 15) in the same PR as the YAML fix, and address the 3 pre-existing test failures in their own PRs. See D8 + D9 + plan. |
