{
	"name": "@bramburn/pi-clipboard-rs",
	"version": "0.84.5",
	"description": "Fork-local cross-platform clipboard native addon (Rust + napi-rs) for pi-coding-agent. Replaces @mariozechner/clipboard. Built on all 6 target platforms by .github/workflows/clipboard-rs-build.yml.",
	"type": "module",
	"main": "./index.js",
	"types": "./index.d.ts",
	"exports": {
		".": {
			"types": "./index.d.ts",
			"import": "./index.js"
		}
	},
	"files": [
		"index.js",
		"index.d.ts",
		"prebuilds",
		"README.md",
		"CHANGELOG.md"
	],
	"os": [
		"linux",
		"darwin",
		"win32"
	],
	"cpu": [
		"x64",
		"arm64"
	],
	"scripts": {
		"clean": "shx rm -rf prebuilds *.node",
		"build:napi": "napi build --platform --release --strip --binary-name clipboard-rs --js index.js --dts index.d.ts",
		"build:local": "npm run build:napi",
		"build:all": "echo 'multi-platform build is run in CI; locally use build:local'",
		"check": "tsgo --noEmit",
		"test": "node --test test/*.test.mjs"
	},
	"devDependencies": {
		"@napi-rs/cli": "3.5.0",
		"shx": "0.4.0",
		"typescript": "5.9.3"
	},
	"keywords": [
		"clipboard",
		"native",
		"rust",
		"napi",
		"napi-rs",
		"addon",
		"pi"
	],
	"author": "bramburn",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/bramburn/pi.git",
		"directory": "packages/clipboard-rs"
	},
	"engines": {
		"node": ">=22.19.0"
	}
}
